const fallback='https://github.com/hanifb1360/Folioflow-Releases/releases';
const links=['download','nav-download','closing-download'].map(id=>document.getElementById(id));
fetch('https://api.github.com/repos/hanifb1360/Folioflow-Releases/releases').then(r=>r.ok?r.json():Promise.reject()).then(releases=>{const release=releases.find(item=>!item.draft&&item.assets?.some(asset=>asset.name.toLowerCase().endsWith('.dmg')));const asset=release?.assets.find(item=>item.name.toLowerCase().endsWith('.dmg'));if(!release||!asset)return;links.forEach(link=>{if(link)link.href=asset.browser_download_url});document.querySelectorAll('.button-label').forEach(label=>{label.textContent=`Download ${release.tag_name}`});const note=document.getElementById('release-label');if(note)note.textContent=`${release.tag_name} · Apple Silicon · Export engine included`}).catch(()=>links.forEach(link=>{if(link)link.href=fallback}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(element=>observer.observe(element));
const shot=document.getElementById('product-shot');document.querySelectorAll('.shot-tab').forEach(tab=>tab.addEventListener('click',()=>{if(!shot||tab.classList.contains('active'))return;document.querySelectorAll('.shot-tab').forEach(item=>item.classList.remove('active'));tab.classList.add('active');shot.classList.add('changing');const preload=new Image;preload.onload=()=>{shot.src=tab.dataset.shot;shot.alt=tab.dataset.alt;requestAnimationFrame(()=>shot.classList.remove('changing'))};preload.src=tab.dataset.shot}));
const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',event=>{if(glow)glow.style.transform=`translate(${event.clientX-210}px, ${event.clientY-210}px)`},{passive:true});document.getElementById('year').textContent=new Date().getFullYear();
